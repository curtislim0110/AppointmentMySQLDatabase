package helper;

/**
 * Interface for Lambda Expression #1 used in CustomersController.java. This Lambda Expression is slightly
 * shorter than the normal java coding and could be implemented in multiple classes as needed for different
 * types of alerts and errors that take a integer parameter.
 * @param alertNumber ia used to select between different types of alerts
 */
public interface lambdaAlertInterface {
    void lambdaAlertMethod(int alertNumber);
}
